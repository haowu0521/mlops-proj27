# Serving deps
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
mlflow==2.19.0
pandas==2.2.3
boto3==1.35.36

# Model flavor deps. Pinned to a narrow 4.x range to bracket both:
#   - 4.45.2 (too old): lacks transformers.core_model_loading referenced
#     by the pickle => ModuleNotFoundError at load.
#   - 5.x (too new): dropped the "summarization" pipeline task in the
#     registry refactor => KeyError when load_context calls pipeline(...).
# 4.48.x still has both; adjust if MLflow's model/conda.yaml captured a
# different exact training version.
# NOTE: torch is installed separately in the Dockerfile from the CPU-only
# wheel index to avoid pulling ~2GB of CUDA runtime we will never use on
# the k3s CPU node.
transformers>=4.48,<5
sentencepiece
