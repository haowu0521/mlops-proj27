# Serving deps
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
mlflow==2.19.0
pandas==2.2.3
boto3==1.35.36
requests==2.32.3

# Model flavor deps. faster-whisper bundles ctranslate2 internally and does
# NOT need torch — that's why this requirements file is much smaller than
# the BART summarizer's. Pin to a 1.x release that matches what
# training/register_asr_model.py declared in pip_requirements.
faster-whisper==1.0.3
